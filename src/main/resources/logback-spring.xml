<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">
    
    <!-- 引入 Spring Boot 默认配置 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <!-- 从 application.yml 读取配置 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="smart-learning"/>
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name" defaultValue="logs/application.log"/>
    <springProperty scope="context" name="LOG_PATTERN_CONSOLE" source="logging.pattern.console"
                    defaultValue="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx"/>
    <springProperty scope="context" name="LOG_PATTERN_FILE" source="logging.pattern.file"
                    defaultValue="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } --- [%t] %-40.40logger{39} : %m%n%wEx"/>
    
    <!-- ============================================ -->
    <!-- 控制台输出配置 -->
    <!-- ============================================ -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN_CONSOLE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- ============================================ -->
    <!-- 文件输出配置 - 所有日志 -->
    <!-- ============================================ -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 正在记录的日志文件 -->
        <file>${LOG_FILE}</file>
        <!-- 日志格式 -->
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 滚动策略：按日期和大小滚动 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 归档日志文件名格式 -->
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- 单个文件最大大小 -->
            <maxFileSize>100MB</maxFileSize>
            <!-- 保留天数 -->
            <maxHistory>30</maxHistory>
            <!-- 所有归档日志总大小 -->
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    
    <!-- ============================================ -->
    <!-- 文件输出配置 - ERROR 级别 -->
    <!-- ============================================ -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.error</file>
        <!-- 只记录 ERROR 级别 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>${LOG_PATTERN_FILE}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    
    <!-- ============================================ -->
    <!-- 异步日志配置 - 提升性能 -->
    <!-- ============================================ -->
    <appender name="ASYNC_FILE_ALL" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 不丢失日志：默认如果队列 80% 满，会丢弃 TRACE、DEBUG、INFO 级别日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 队列大小 -->
        <queueSize>512</queueSize>
        <!-- 添加附加的 appender -->
        <appender-ref ref="FILE_ALL"/>
    </appender>
    
    <appender name="ASYNC_FILE_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>256</queueSize>
        <appender-ref ref="FILE_ERROR"/>
    </appender>
    
    <!-- ============================================ -->
    <!-- Logger 配置 -->
    <!-- ============================================ -->
    
    <!-- Spring 框架日志 -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.boot" level="INFO"/>
    
    <!-- MyBatis-Plus 日志 -->
    <logger name="com.baomidou.mybatisplus" level="INFO"/>
    
    <!-- 数据库连接池日志 -->
    <logger name="com.zaxxer.hikari" level="INFO"/>
    
    <!-- Redis 日志 -->
    <logger name="io.lettuce.core" level="WARN"/>
    <logger name="org.springframework.data.redis" level="INFO"/>
    
    <!-- RabbitMQ 日志 -->
    <logger name="org.springframework.amqp" level="INFO"/>
    <logger name="com.rabbitmq" level="WARN"/>
    
    <!-- 业务日志 -->
    <logger name="cloud.zimometaverse.backend" level="INFO"/>
    
    <!-- ============================================ -->
    <!-- 根 Logger -->
    <!-- ============================================ -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE_ALL"/>
        <appender-ref ref="ASYNC_FILE_ERROR"/>
    </root>
    
    <!-- ============================================ -->
    <!-- 开发环境配置 -->
    <!-- ============================================ -->
    <springProfile name="dev">
        <logger name="cloud.zimometaverse.backend" level="DEBUG"/>
        <logger name="cloud.zimometaverse.backend.mapper" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
    
    <!-- ============================================ -->
    <!-- 测试环境配置 -->
    <!-- ============================================ -->
    <springProfile name="test">
        <logger name="cloud.zimometaverse.backend" level="INFO"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE_ALL"/>
            <appender-ref ref="ASYNC_FILE_ERROR"/>
        </root>
    </springProfile>
    
    <!-- ============================================ -->
    <!-- 生产环境配置 -->
    <!-- ============================================ -->
    <springProfile name="prod">
        <logger name="cloud.zimometaverse.backend" level="INFO"/>
        <logger name="cloud.zimometaverse.backend.mapper" level="WARN"/>
        <root level="WARN">
            <appender-ref ref="ASYNC_FILE_ALL"/>
            <appender-ref ref="ASYNC_FILE_ERROR"/>
        </root>
    </springProfile>
    
</configuration>

